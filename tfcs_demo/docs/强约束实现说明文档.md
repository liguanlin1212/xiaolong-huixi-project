# 强约束实现说明文档

## 重要说明（必须遵守）
本文档是 Demo 的 **最高实现约束**。
Trae 在生成代码、补全模块、做架构推断时：
❌ 不允许自行扩展功能
❌ 不允许增加未定义模块
❌ 不允许引入"未来系统"设计
✅ 只允许严格实现本文档中明确要求的内容

## 0. Demo 实现总目标（不可偏离）

### Demo 的唯一目标
在端侧，对给定舆论文本进行自动分类：
情绪叙事 / 证据叙事 / 法律叙事，
并在时间轴中展示叙事类型的演化。

### 明确不做的事情（禁止事项）
❌ 不判断事实真伪
❌ 不裁决对错
❌ 不做推荐 / 个性化
❌ 不做多事件系统
❌ 不做完整数据库 / 后端系统

## 1. Demo 产品范围（严格限定）

### Demo 只包含 1 个案例
案例名称： 刘鑫江歌案
判断对象：
刘鑫是否对江歌之死负有责任（法律 / 道德）

### Demo 只包含 1 个 AI 能力
文本叙事类型分类
- 情绪叙事（Emotional）
- 证据叙事（Evidence）
- 法律叙事（Legal）

## 2. 系统总体架构（强制采用）

### 分层架构（禁止跨层调用）
Presentation Layer（展示层）
└── 时间轴 UI / 文本结果展示

Application Layer（业务层）
└── 叙事分析与时间线生成逻辑

AI Capability Layer（AI 能力层）
└── 端侧文本分类推理（NPU / CPU）

Data Layer（数据层）
└── Demo 案例数据与结果

❗ 禁止 UI 直接调用 AI 层
❗ 禁止 AI 层知道业务语义（如"责任"）

## 3. 强制目录结构（必须一致）
Trae 生成代码时， 目录结构必须完全一致 ：
tfcs_demo/
├── app/
│   ├── ui/
│   │   └── timeline_view.py
│   │
│   ├── api/
│   │   └── analyse.py
│   │
│   └── services/
│       ├── narrative_service.py
│       └── judgement_service.py
│
├── ai/
│   ├── models/
│   │   └── narrative_classifier.onnx
│   │
│   ├── inference/
│   │   └── npu_runner.py
│   │
│   └── schema.py
│
├── core/
│   ├── enums.py
│   └── types.py
│
├── data/
│   ├── raw_texts.json
│   └── results.json
│
├── infra/
│   └── device.py
│
└── docs/
    └── demo_explain.md

## 4. 核心数据与类型约束（必须先实现）

### 4.1 枚举定义（core/enums.py）
```python
from enum import Enum

class NarrativeType(Enum):
    EMOTIONAL = "情绪叙事"
    EVIDENCE = "证据叙事"
    LEGAL = "法律叙事"
```

❗ 不允许新增枚举
❗ 不允许更名

### 4.2 核心数据结构（core/types.py）
```python
from dataclasses import dataclass
from core.enums import NarrativeType

@dataclass
class TextEntry:
    time: str
    text: str

@dataclass
class AnalysedEntry:
    time: str
    text: str
    narrative: NarrativeType
    confidence: float
```

## 5. AI 能力层约束（最重要）

### 5.1 AI 模型职责（严格限定）
AI 模型 **只负责一件事**：
输入一段文本 → 输出叙事类型 + 置信度

### 5.2 模型接口契约（ai/schema.py）
```python
class NarrativeResult:
    label: str        # EMOTIONAL / EVIDENCE / LEGAL
    confidence: float # 0.0 ~ 1.0
```

### 5.3 推理接口（ai/inference/npu_runner.py）
```python
def classify_text(text: str) -> dict:
    """
    输入：
        text: 舆论文本（str）

    输出：
        {
          "label": "EMOTIONAL" | "EVIDENCE" | "LEGAL",
          "confidence": float
        }
    """
    pass
```

### 允许的实现方式（Demo 阶段）
✅ Mock / 规则实现
✅ CPU 推理
✅ NPU 推理（优先）

### 禁止行为
❌ AI 输出"责任判断"
❌ AI 输出多标签
❌ AI 引入外部网络调用

## 6. 业务层实现逻辑（强约束）

### 6.1 narrative_service.py
职责：
- 调用 AI 推理
- 将原始文本转为结构化分析结果

```python
from ai.inference.npu_runner import classify_text
from core.enums import NarrativeType

def analyse_text_entry(entry: dict) -> dict:
    result = classify_text(entry["text"])
    return {
        "time": entry["time"],
        "text": entry["text"],
        "narrative": NarrativeType[result["label"]],
        "confidence": result["confidence"]
    }
```

### 6.2 judgement_service.py
职责：
- 生成"认知演变时间轴"
- 不做任何 AI 推理

```python
def build_timeline(analysed_entries: list) -> list:
    """
    输入：按时间排序的 AnalysedEntry
    输出：时间轴结构（list）
    """
    pass
```

## 7. 数据层约束（Demo 专用）

### raw_texts.json 示例结构
```json
[
  {
    "time": "2016-11",
    "text": "刘鑫也是受害者，她当时也很害怕"
  },
  {
    "time": "2017-01",
    "text": "聊天记录显示她提前意识到危险"
  },
  {
    "time": "2022-01",
    "text": "法院判决认定不构成刑事责任"
  }
]
```

❗ 时间字段必须存在
❗ 文本必须是自然语言

## 8. UI 层要求（最低实现）

### timeline_view.py
UI 只需做到 ：
- 按时间顺序展示：
  - 时间
  - 文本
  - 叙事类型（标签）

不需要复杂交互
不需要搜索 / 编辑

## 9. Demo 验收标准（Trae 的完成判定）
Trae 的实现 **只有在满足以下条件时才算完成**：
- 程序可运行
- 给定 raw_texts.json
- 本地完成文本分类
- UI 能展示叙事类型随时间变化
- AI 分类逻辑 不依赖网络

## 10. 严禁 Trae 擅自做的事情（再次强调）
❌ 增加推荐 / 排序逻辑
❌ 引入数据库
❌ 扩展为多事件系统
❌ 修改判断对象定义
❌ 引入"未来规划"代码

## 11. 实现顺序建议（Trae 应遵守）
1. core/
2. data/
3. ai/（先 mock）
4. services/
5. ui/
6. 再考虑 NPU 接入

## 最终说明
这是一个"结构优先、AI 单点、Demo 级"的工程项目。
请严格实现，不要聪明过头。
